## v1.1.0

### ✨ 新功能

- **🎬 视频编码兼容性增强：HEVC/H265 与冷门容器自动转码播放**
  - HEVC/H265 编码视频：先尝试浏览器硬件解码，失败后自动调用 ffmpeg 转码为 H264 播放
  - TS/MTS/M2TS 容器格式：自动流式拷贝或重编码为 MP4 播放（Chromium 原生不支持）
  - 不支持的视频编码：自动检测并转码，播放器控制栏实时显示「转码中 X%」进度
  - 转码伴随缓存：输出 `.transcoded.mp4` 到原视频同目录，下次播放直接命中缓存跳过转码
  - 转码中断保护：插件卸载时清理不完整的伴随 MP4，防止下次启动加载损坏文件卡死浏览器
  - ffprobe 编码检测缓存（`_probeCache`）：避免重复探测同一文件的编码信息
  - 慢速存储智能跳过：网盘等慢速存储自动跳过同步 ffprobe 检测，用默认编码猜测避免长时间阻塞
  - ffmpeg 路径自动检测：启动时检查 `C:\ffmpeg\bin\ffmpeg.exe` 并加入 PATH
- **🔧 跨端同步文件夹配置改造：去掉强制 LocalBrowseSync，用户可自主命名**
  - 用户直接指定同步文件夹路径，不再强制拼接 `LocalBrowseSync` 子文件夹
  - 各设备的同步文件夹名需一致（修复靠前缀替换：父路径不同，子路径一致），但可自定义任意名称
  - 新增盘符根目录校验：拒绝 `D:\`、`C:`、`/` 等过浅路径，防止全盘前缀替换误伤
  - 文件夹不存在时自动创建（需父目录存在）
  - UI 文案更新：配置面板描述、placeholder、按钮提示均改为「选择同步文件夹」
  - 旧配置完全兼容：修复算法只做前缀匹配不挑文件夹名，已配置 `LocalBrowseSync` 的用户升级后照常工作

### 🐛 Bug 修复

- **修复 `_getMySyncRoot` 中 `that._log` 引用错误**：方法内未定义 `that` 变量，deviceId 变更迁移分支会抛 `ReferenceError`。改为 `this._log`。触发场景：首次加载 deviceId 为 `'unknown'`，后续变为真实主机名时
- **修复转码轮询定时器未清除的问题**：切换视频时旧定时器继续运行，已通过 `clearInterval(this._transcodePollInterval)` 清除
- **修复切换视频后转码进度残留显示的问题**：隐藏旧视频的 `#cd-video-transcode-status` 元素

### 📝 文档

- README 中英文「跨端同步」章节同步更新：配置步骤、示例表格、FAQ 三处均改为新机制
- 保留 v0.6.0 changelog 中的 `LocalBrowseSync` 历史记录（历史事实）

## v1.0.0

### ✨ 新功能

- **🎬 音视频增强：时间戳与字幕智能跟踪**
  - 音视频打开方式全面升级，支持时间戳跳转和字幕智能跟踪
  - 新增文档/图片/代码类文件打开方式设置（⚙️ 设置页面）
  - 视频截图优化：PNG → JPEG，限制最大宽度 1920px，减小体积
- **🔍 搜索升级**
  - 深度搜索新增停止按钮，搜索过程中可随时停止
  - 搜索历史跨重启持久化，可收藏常用搜索（☆→★）
  - 深度搜索结果中文件夹路径可直接点击跳转
  - 深度搜索支持 dot-prefix 文件夹（如 `.git`、`.vscode`）

### 🐛 Bug 修复

- 修复搜索收藏静默失败的问题
- 修复 `_windowVideoInterceptor` 缺少 `localbrowser` 分支的问题
- 修复 FLV 对话框内存泄漏
- 修复文档中非音视频链接误调播放器的问题
- 修复 `.md` 等文件链接打不开的问题

### 📝 文档

- README 改为中文为主
- 新增视频播放器完整使用教程
- 文档结构重构
- 捐赠区添加收款码

## v0.9.0

### ✨ 新功能

- **⭐ 收藏夹全面重构：从平铺标签升级为「顶部快捷栏 + 悬浮树形分组」多级结构**
  - 数据结构升级：收藏项新增 `groupId`、`pinToTop` 字段，新增 `favoriteGroups` 分组数组，支持无限层级扩展
  - 顶部快捷栏：工具栏区域显示最近浏览的 5 个收藏文件夹，一键快速访问
  - 悬浮收藏树面板：鼠标悬浮 ⭐📁 按钮自动展开 `#cd-fav-tree-wrap` 面板，移出后自动收起
  - 分组管理：支持折叠/展开、右键菜单、新建/重命名/删除分组，分组名称颜色跟随思源主题色（`var(--b3-theme-primary)`）
  - 拖拽功能：收藏项拖入分组移动、分组之间拖拽排序、同分组内收藏项拖拽排序
- **⚙️ 新增设置页面**
  - 插件卡片右下角齿轮图标打开设置面板
  - 📁 **文件夹打开方式**：插件默认（在插件面板中打开）/ 电脑默认（系统资源管理器打开）
  - 🎬 **视频打开方式**：思源默认（思源笔记打开）/ 电脑默认（系统播放器打开）
  - 首次安装默认：文件夹为「插件默认」，视频为「思源默认」
- **🎵 音频文件封面图显示**
  - 图标视图下，音乐文件优先显示目录级封面图（自动检测 `cover.jpg` / `folder.jpg` / `album.jpg` / `front.jpg` 及 `.png` / `.jpeg` / `.webp` 变体）
  - 目录无封面时，自动从 MP3 的 ID3v2.3/2.4 标签解析 APIC 帧提取内嵌封面，转为 base64 实时展示
  - 缩略图队列异步处理音频封面，不阻塞图片缩略图加载流
- **🔧 收藏路径自动修复**
  - 启动时自动遍历检查所有收藏路径的有效性
  - 文件夹被移动后，按优先级在常见位置搜索同名文件夹并自动修复路径：父目录及其子目录 → 祖父目录 → 曾祖父目录 → 常见用户目录（Desktop/Documents/Downloads 等）→ Windows 各盘符根目录
  - 唯一匹配原则：仅找到 1 个匹配时才自动修复，多个匹配时保留原路径避免误匹配
  - 修复成功/失败均提供 Toast 反馈

### 🐛 Bug 修复

- **修复 system 模式打开文件时双开窗口的问题**：增加 `_lastOpenedFile` 500ms 去重机制，防止短时间内同一文件被重复调用系统打开
- **修复点击文档中的文件/PDF/视频链接时未在插件面板定位的问题**：`localbrowser` / `system` / `siyuan` 三种模式下，打开资源前均先调用 `locateFileInPanel` 同步定位并高亮
- **修复启动时收藏路径检查重复保存的问题**：`_checkFavoritesValidity` 改为路径 `_exists` 状态真正变化时才设置 `changed=true`，避免无意义的写盘
- **修复定位失败提示文案错误**：`_doLocateFile` 失败提示从「未在当前列表中找到该音频」改为通用提示「未在当前列表中找到该文件」

### 💄 UI 优化

- 收藏树面板分组展开箭头样式优化，更加美观
- 分组名称颜色跟随思源主题主色，保持与播放器播放按钮等核心元素视觉统一
- 新建分组交互优化：输入名称后回车即可保存，操作更自然
- 右键菜单移除「删除收藏」入口（避免误触，保留面板内拖拽和分组管理作为替代）

## v0.8.1

### 🐛 Bug 修复

- **修复深度搜索结果中文件大小和修改时间缺失的问题**：`var item` 闭包变量在 for 循环中被共享，导致除每个目录最后一个匹配文件外，其他文件的 size/mtime 均为 undefined
- **修复 Dock 面板长时间闲置后重建时图标视图崩溃的问题**：`_boundIconScroll` 在 destroy 时被设为 null，重建后 `addEventListener('scroll', null)` 抛出 TypeError
- **修复列表视图下图片预览不显示文件大小和修改时间的问题**：查找文件信息时仅检查 `iconRenderState`，列表视图下该对象为 null
- **修复启动时重复加载目录的问题**：路径恢复逻辑引用不存在的 `#cd-path` 元素，导致 `loadPathSettings` 异步回调始终触发多余的 `loadDirectory`（引入 `_lastRenderedPath` 追踪实际渲染路径）
- **修复启动时滚动位置保存到错误路径的问题**：`_navigateToSyncRootOnLoad` 在 `loadDirectory` 前手动设置 `this.currentPath`，导致滚动位置被保存到错误的路径键下

### ⚡ 性能优化

- **优化启动加载：记住上次关闭时的目录**：打开插件时优先恢复上次浏览的目录，而非每次都导航到同步文件夹根目录
- **减少启动时多余的目录加载**：路径恢复判断改用 `_lastRenderedPath` 和 `_pathRestoredFromSettings` 标志，避免异步竞态导致的重复 `loadDirectory` 调用

## v0.8.0

### ✨ 新功能

- **📦 内部资源管理器（Assets Manager）**
  - 新增「📦 内部资源」Tab，直接浏览思源笔记 `assets/` 目录中的文件
  - 支持按类型过滤：图片 / 视频 / 音频 / 文档 / 其他
  - 支持按扩展名快速筛选（顶部格式标签栏：PDF / XLSX / DOCX / ZIP 等）
  - 支持实时搜索资源文件名
  - 每个资源显示引用计数（被多少个文档引用）
  - 双击资源打开引用该资源的文档
  - 右键菜单
  - 图片资源悬停预览
  - **支持列表/图标双视图切换**，根级笔记本和子文档层级均可切换

### 🐛 Bug 修复

- **修复 assets 子目录文件无法打开的问题**：`assets/` 下存在子目录（如 `memos/`、`covers/`）时，右键「打开文件/打开文件位置」因路径拼接错误导致 Windows 找不到文件。现已记录文件相对路径并正确拼接完整路径
- **修复双击资源偶尔无反应的问题**：`_assetBlockMap`（资源 → 块 ID 映射）在缓存加载后丢失，导致双击时进入异步 SQL 查询，`window.open` 可能被浏览器拦截。现已将 `_assetBlockMap` 持久化到 localStorage 缓存，恢复后双击即可同步打开文档
- **提升资源关联查询上限**：`assets` 表和 `blocks` 内容扫描的 SQL LIMIT 从 10000 提升至 100000，减少大规模笔记库中资源关联遗漏

## v0.7.0

### ✨ 新功能

- **🖱️ 拖拽插入链接**
  - 从浏览器面板直接拖拽文件/文件夹到文档中，即可插入链接
  - 支持 Ctrl/Shift+点击多选文件，批量拖拽插入多个链接
  - 批量插入时每个文件独占一个块（竖排显示）
  - 智能定位：拖到编辑器内容区 → 插入到鼠标光标位置；拖到空白区域 → 追加到文档底部
- **🔗 文件关联文档**
  - 右键"关联当前文档"自动在文档首行插入文件链接
  - 文件移动后关联图标（🔗）保持不变，自动更新路径映射
- **📍 点击链接定位**
  - 文档中点击文件链接 → 打开文件的同时，浏览器面板自动导航并高亮定位该文件
  - 文档中点击文件夹链接 → 在浏览器面板中打开文件夹，不再弹出系统资源管理器
- **📦 外部拖拽复制文件**
  - 支持从系统文件管理器直接拖拽文件到插件面板，复制到当前目录或目标文件夹

### 🐛 Bug 修复

- 修复文件关联文档后，移动文件到其他文件夹导致关联图标消失的问题
- 修复外部拖拽文件时 dragover 显示禁止图标的问题（改用 dataTransfer.types 检测）
- 修复新版 Electron 中 file.path 为空导致拖拽复制失败的问题（增加 webUtils.getPathForFile 和 FileReader 回退方案）
- 修复文件复制后目录不自动刷新的问题
- 修复拖拽复制后虚线边框持续显示的问题
- 修复 dragleave 在子元素间移动时误触发的问题
- 修复 loadDirectory 重复绑定拖拽事件的问题（增加 _cdDragEventsBound 标志）

## v0.6.2

### ✨ 新功能

- **🎵 新增音乐播放器**
  - 支持播放本地音频文件（MP3/WAV/FLAC/AAC/OGG/M4A/WMA 等），双击即可播放
  - 内置音乐播放器：播放/暂停、上一首/下一首、进度条拖拽、音量调节（自动记忆）
  - 实时歌词显示：支持 LRC 同步歌词，自动搜索同名歌词文件，点击歌词跳转播放进度
  - 封面展示：自动提取 MP3 内嵌封面，目录浏览时预加载封面图片
  - 歌词背景：模糊封面图作为歌词背景，营造沉浸式播放体验
  - 封面/歌词平滑过渡：交叉淡入淡出（crossfade）切换动画
  - 下一首预加载：自动预加载下一首音频文件及封面，切歌零延迟
  - 播放列表：自动识别当前目录所有音频文件，支持按文件夹连续播放

### 🐛 Bug 修复

- 修复 Buffer.subarray() 底层 ArrayBuffer 泄露导致音频封面乱码的问题
- 修复 blob URL 被 DOM 引用时立即 revoke 导致封面消失的问题
- 修复切歌时歌词面板闪烁（innerHTML 改为 DocumentFragment 原子替换）
- 修复切歌时封面不更新的问题（错误提前 return）
- 修复首次启动歌词不显示的问题（fragment.childNodes 在 replaceChildren 后为空）
- 修复异步回调竞态导致旧音频数据覆盖新音频的问题（epoch 世代计数器 + 路径保护）
- 修复 crossfade 切歌竞态导致封面错乱的问题
- 修复 onunload 未清理 pending blob URL 导致内存泄漏的问题

### 💄 UI 优化

- 歌词按钮有歌词时自动高亮，无歌词时置灰
- 网盘大音频文件预加载优化（metadata 优先 + 事件驱动加载）
- 统计信息栏单行显示，防止文字换行
- 播放/暂停按钮使用 SVG 图标，支持 loading 旋转动画
- 切歌 loading 闪烁消除（延迟显示 + 按压缩放反馈）
- 音量设置持久化（localStorage 存储，重启后恢复）
- 跨文件夹切歌支持（正确补全文件路径）

### ⚡ 性能优化

- Canvas 预生成 80×80 模糊缩略图，避免大图实时 blur 运算
- 歌词背景延迟彻底消除（预模糊缩略图直接使用，去掉 CSS filter:blur）
- 下一首音频文件及封面预加载机制，切歌即时响应

## v0.6.1

### 🐛 Bug 修复

- 修复 macOS APFS firmlink 导致同一文件返回两个路径（`/Users/...` 和 `/System/Volumes/Data/Users/...`），搜索时重复扫描且用户需在"同一文件"间手动选择的问题
  - 搜索递归时跳过 `/System/Volumes/Data` 目录，避免重复扫描
  - 搜索结果自动去重（`_normalizeMacPath` + `_dedupCandidates`）

### 🔧 其他

- 移除底部状态栏 🐛 debug 按钮，调试日志默认关闭
- 保留 `localStorage.setItem('cd_debug', 'true')` 控制台手动开启方式

## v0.6.0

### ✨ 新功能

- **🔄 跨端同步文件夹升级：支持同平台多设备**
  - 新增支持同平台跨设备场景（如两台 Windows 电脑不同盘符：`D:\BaiduSyncdisk` ↔ `E:\BaiduSyncdisk`）
  - 数据格式升级：每平台从单条路径变为按设备（主机名）存储，多台同平台设备不再互相覆盖
  - 完整路径前缀匹配替代文件夹名匹配，更加精确，不会误判同名文件夹
  - 旧格式自动迁移：迁移前验证路径在当前设备是否实际存在，防止其他设备的路径被错误归到本机
  - 设置面板简化：只显示当前设备配置 + 平台胶囊标签，去掉其他平台未配置提示
- **📂 强制统一同步文件夹名称为 LocalBrowseSync**
  - 浏览按钮选择网盘同步目录的父文件夹，自动创建 LocalBrowseSync 子文件夹
  - 所有设备统一使用 LocalBrowseSync 作为同步文件夹，无需手动确保名称一致
- **🏠 启动时默认显示跨端同步文件夹**
  - 打开思源笔记时优先导航到跨端同步文件夹根目录，防止上次保存的子目录因文件移动而失效
  - 未配置同步文件夹时回退到上次路径或盘符根目录
- **☁️ macOS 云盘快捷方式**
  - 目录选择器中自动扫描主目录下的百度网盘、百度网盘同步空间等云盘文件夹
- **🐳 Docker/鸿蒙 API 双模式**
  - 新增 API 模式（鸿蒙 HarmonyOS 适用的只读模式），自动检测 Docker 容器环境
  - API 模式下禁用文件操作（复制到 assets、右键插入等），file:/// 链接自动屏蔽
  - Docker 版平台和设备 ID 正确识别（读取 `window.siyuan.config.system` 多字段）

### 🐛 Bug 修复

- 修复同平台跨设备链接不走跨端修复通道的问题（`_isCurrentPlatformLink` 返回 true 时跳过了 `_isInSyncFolder` 检查）
- 修复深度搜索递归未过滤隐藏文件（`.` 开头、`$RECYCLE.BIN`、`System Volume Information`）
- 修复 `_isInSyncFolder` 基于名称匹配导致误判（改为精确前缀 + 段边界检查）
- 修复 `_crossSyncRepair` 前缀匹配不够精确（`BaiduSyncdisk` 可能匹配 `BaiduSyncdiskBackup`）
- 修复路径拼接末尾多余 `/`（路径恰好是 syncRoot 本身时）
- 修复 `goUp()` 在 Unix 挂载点路径刷新原地（`/Volumes` goUp 到 `/`）
- 修复 `syncRoots` 异步加载与保存的数据竞争（添加 `_syncRootsLoaded` 标志）
- 修复 macOS 浏览按钮点击无反应（osascript 静默失败，改为直接使用内置 `pickDirectory()`）
- 修复 `pickDirectory` 选择目录后关闭同步配置面板（添加 `e.stopPropagation()` 阻止事件冒泡）
- 修复跨平台路径污染：macOS 使用后 Windows 显示空目录（`loadPathSettings` 增加平台路径校验 + `fs.existsSync` 验证）
- 修复跨设备路径污染：另一台 Windows 设备打开时显示空目录（旧格式迁移 + 新格式读取均增加 `fs.existsSync` 验证，无效记录自动清理）
- 修复 `loadDriveSettings` 盘符跨设备同步导致无效盘符（增加 `fs.existsSync` 校验，localStorage 降级同样校验）
- 修复 `_migrateSyncRoots` 当前平台旧格式迁移将其他设备路径错误归到本机（增加 `fs.existsSync` 验证）
- 修复 `_syncRootInlineCloseBound` 内存泄漏（保存引用并在 `onunload` 清理）

## v0.5.6

### ✨ 新功能

- **🔗 失效链接修复升级为红黄绿三灯指示 + 无感自动修复**
  - 工具栏指示灯实时显示链接状态：⚪白灯（无本地链接）/ ⏳转圈（检测中）/ 🟢绿灯（全部有效）/ 🟡黄灯（重名需手动选择）/ 🔴红灯（无法修复）
  - 打开文档时自动后台扫描并修复失效链接，单结果自动替换，无需任何操作
  - 多候选结果黄灯提示，点击指示灯手动选择
  - 无结果红灯提示，提示手动处理

- **📂 双击文件夹插入文件夹链接**：双击文件夹自动插入 `[📂 文件夹名](file:///路径)` 格式的链接

### 🐛 Bug 修复

- 修复 `parseInt` 缺少 radix 参数导致的潜在解析错误（11 处）
- 修复 `decodeURIComponent` 遇到非法编码时崩溃的问题
- 修复 `candidateArea` 为 null 时访问 DOM 属性报错的问题
- 修复 `localStorage` 中 JSON.parse 异常导致收藏夹加载失败的问题

### 💄 UI 优化

- 失效链接修复对话框图标和文字优化（标题🔧、失效🔗、未找到⚠️）
- URL 参数不再干扰文件名提取（`.split('?')[0]`）
- 收藏夹拖拽排序优化（DOM 交换替代重建，消除闪烁）
- 首次展开文件夹去掉"加载中..."闪烁
- 非图片/视频文件插入时显示原文件名 + 文件类型图标

### 🔧 其他

- 更新插件文档说明（README.md / README_zh_CN.md）

## v0.5.5

### ✨ 新功能

- **🧲 失效链接修复**：一键扫描并修复文档中所有失效的本地文件链接
  - 三级搜索策略：R1 原目录就近找 → R2 上级目录扩大搜 → R3 全盘深度搜
  - 文件指纹匹配：通过 size + mtime 精确识别同名文件
  - 自动修复 + 手动确认：单结果自动修复，多结果展示候选列表供选择
  - 批量并行处理：多个失效链接同时搜索，对话框实时显示进度

- **📎 文件夹右键"添加到文档"**：右键文件夹可直接将文件夹路径以 Markdown 链接形式插入到当前文档

### 🐛 Bug 修复

- 修复并行搜索时 `deepSearch` 互相取消导致搜索结果丢失的问题
- 修复 `detectDrives` 检测网络盘时可能永久卡住的问题（增加 5 秒超时）
- 修复循环闭包变量共享导致错误日志指向错误索引的问题
- 修复图标滚动事件监听器重复添加导致的内存泄漏
- 修复 `replaceLink` 正则全局替换可能误伤同一块中其他链接的问题
- 修复 `result.broken` 为 undefined 时访问 `.length` 抛异常的问题
- 修复确认替换按钮未 await 导致的竞态条件
- 修复修复后新链接丢失文件指纹 title 的问题
- 修复 OTHER 类型 Dirent（symlink/junction）未正确处理的问题
- 修复排序菜单过滤逻辑与搜索框不一致的问题
- 修复 relink 对话框关闭后后台搜索仍在运行的问题
- 修复 toast 定时器未在 onunload 中清理的问题

### 💄 UI 优化

- 候选区域字体颜色改为黑色，提升可读性
- 日期格式改为 `年/月/日`，大小和日期用不同颜色区分
- 候选列表背景色统一为绿色，去除第一行蓝色高亮
- "指纹匹配"标签加上括号
- "一键修复"按钮改为纯状态展示（不可点击），避免重复触发
- 失效/有效链接数量分别用红色/绿色高亮显示
- 工具栏失效链接修复按钮图标改为 🧲 磁铁

### 🔧 其他

- 文件夹添加到文档的链接前显示 📂 图标
- 文件指纹信息从 URL fragment 迁移到 Markdown title 属性
- 搜索进度提示从 Toast 弹窗迁移到对话框内候选区域
- 并行处理多个失效链接，提升修复效率
